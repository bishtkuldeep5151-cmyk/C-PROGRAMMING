#include <stdio.h>
    int main()

    
    {
        int a, b, sum;
        printf("Enter two numbers: ");
        scanf("%d %d", &a ,&b);
        sum = a + b;
        printf("%d is the sum of two numbers", sum);
        return 0;
    }